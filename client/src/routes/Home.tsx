import React from 'react'
import {
    Container,
} from '@chakra-ui/react'

interface Props {

}

const Home = (props: Props) => {
    return (
        <Container maxWidth={1320}>
            <h1>Welcome Home</h1>
        </Container>
    )
}

export default Home
